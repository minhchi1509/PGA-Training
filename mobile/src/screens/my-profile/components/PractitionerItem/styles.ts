import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    gap: 4,
    alignItems: 'center',
    width: 108,
  },
  avatar: {
    height: 56,
    width: 56,
    borderRadius: 56,
  },
  checkFilledIcon: {
    position: 'absolute',
    bottom: 0,
    right: 0,
  },
});

export default styles;
